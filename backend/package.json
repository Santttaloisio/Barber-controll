{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "dev": "set NODE_OPTIONS=--dns-result-order=ipv4first && ts-node-dev --respawn src/server.ts",

    "build": "tsc",

    "build:frontend": "cd ../frontend && npm install && npm run build",

    "build:full": "npm run build:frontend && npm install && tsc",

    "start": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "@types/pg": "^8.20.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.8",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.22.0",
    "sqlite3": "^6.0.1"
  },
  "engines": {
    "node": "18.x"
  }
}